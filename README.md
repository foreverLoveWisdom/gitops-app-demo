# gitops-app-demo

Minimal Rails app used as a throwaway "app repo" for practicing a two-branch
(release -> master) merge relay with `git-pr-release`, paired with
`argocd-learn-demo` for a full GitOps learning loop on a local k3s cluster.
No real workload — practice repo only.
