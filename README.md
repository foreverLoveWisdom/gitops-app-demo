# gitops-app-demo

Throwaway repo for practicing a two-branch (release -> master) merge relay,
paired with `argocd-learn-demo` for a full GitOps learning loop on a local
k3s cluster. No real workload.
